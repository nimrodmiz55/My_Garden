PORT=5000

SUPABASE_URL=https://gofemiazggmjupjhwzme.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_secret_service_role_key_here
SUPABASE_ANON_KEY=sb_publishable_T-p78_1LqQmwXTbBQdW7bA_Jql9D9X0

GEMINI_API_KEY=your_gemini_api_key_here

# Gmail SMTP — used by the daily notification job
# Use a Gmail App Password (not your normal password):
# Google Account → Security → 2-Step Verification → App Passwords
GMAIL_USER=your_gmail_address@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password

# Comma-separated list of allowed CORS origins (production frontend URL + localhost)
# Example: https://my-garden.onrender.com,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# Secret token for the external cron trigger endpoint (POST /api/internal/notify).
# Generate any long random string — e.g. openssl rand -hex 32
CRON_SECRET=your_random_secret_here